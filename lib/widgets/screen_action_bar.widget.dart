import 'package:flutter/material.dart';

class ScreenActionBar extends StatelessWidget {
  const ScreenActionBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Screen Action Bar');
  }
}
